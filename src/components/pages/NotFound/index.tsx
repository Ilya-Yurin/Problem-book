import React from 'react';
import { StyledNotFound404 } from 'components/pages/NotFound/styled';

const NotFound404: React.FC = () => (
  <StyledNotFound404>Page not found</StyledNotFound404>
);

export default React.memo(NotFound404);
